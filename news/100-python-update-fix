### Enhancements

* Use the latest Python spec in prefix history to determine whether a name-only
  `conda update python` can change minor versions, and allow `conda update --all`
  to update Python within the installed minor.

### Bug fixes

### Deprecations

### Docs

### Other
